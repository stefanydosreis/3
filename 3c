#include <stdio.h>
#include <stdlib.h>

int main()
{
    float acertos=0;
    int i, respo,gabarito;
    Char nome[30];
    FILE *aluno,*gab;



    aluno=fopen("C:\\Users\\Aula\\Desktop\\“gabarito.txt","w");
    aluno=fopen("C:\\Users\\Aula\\Desktop\\“aluno.txt","r");
    printf("digite o seu nome:")
    gets(nome);
    fprintf(aluno,"%s\n",nome);

    for(i=0;i<40;i++){

        printf (resposta da questão%d:",i+1);
        scanf("%d",&resp);
        fprint(aluno,"%d\n",respo);
        fscanf(gab,"%d",&gabarito);
        if(respo==rgab)acertos+=1;
}

    fclose(aluno);
    fclose(gab);
    Printf("%s ACERTOU %2f,%\n",nome,(acertos*100)/40)
    return 0;
}
